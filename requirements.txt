openpyxl>=3.1
beautifulsoup4>=4.12
# Zoopla only: drives a headed Chromium through the Cloudflare challenge
# (see scraper/fetch_browser.py). The cloud environment pre-installs the
# browser binary itself; the other platforms run fine without playwright.
playwright>=1.40
